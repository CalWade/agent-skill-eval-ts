# 后期规划

## 当前位置

agent-skill-eval 是一个 **Agent 测试工具**：发 prompt → 收回复 → 判对错 → 出报告。

它解决的是"量化 Agent 表现"的问题，但不解决"怎么改"的问题。改SKILL.md 仍然靠人看报告、人分析、人动手。

下一步的目标是把这条链路闭环：**测试 → 诊断 → 修复 → 验证**，前两步自动化已有，后两步需要一个真正的 Agent 来做。

---

## 两个项目，各自独立

### 项目 A：agent-skill-eval（当前项目）

**定位：测试工具 / 评测引擎**

已完成：
- YAML 驱动的测试执行器
- 三层递进判定（否定上下文感知）
- Trace 数据结构 + OpenAI Adapter
- 多轮 steps 编排引擎
- 半自动失败分类（F3/F4/F5）
- 副作用隔离（side_effect + --safe-only）

待完成：
- [ ] OpenClaw Adapter（解析 EasyClaw 的 SSE stream 格式，拿到每步 Tool Call）
- [ ] 模型对比模式（同一组用例，跑多个模型，自动生成对比表）
- [ ] 裁判校准数据集（golden.yaml + 校准脚本）
- [ ] CI/CD 集成（GitHub Actions，SKILL.md 变更触发回归测试）

### 项目 B：skill-doctor（新项目，Agent 开发）

**定位：基于评测数据的 SKILL.md 自动诊断与修复 Agent**

这是一个真正的 Agent——它自主决策、调用工具、多步执行。

#### 输入

agent-skill-eval 生成的 JSON 测试报告。包含：
- 哪些用例 FAIL 了
- Agent 的完整回复文本
- 判定明细（哪条 criteria 没过）
- 半自动失败分类标签（F1-F7）
- Trace 数据（工具调用链、步骤数、错误码）

#### Agent 能力

| 能力 | 说明 |
|------|------|
| 读取测试报告 | 从 JSON 中筛选 FAIL 用例，按严重程度排序 |
| 读取 SKILL.md | 拿到当前的技能定义文档 |
| 根因分析 | 对比 Agent 实际行为和 SKILL.md 预期流程，定位偏差 |
| 生成修改建议 | 输出具体的 SKILL.md diff（改哪一行、改成什么、为什么改） |
| 自动修改 | 直接编辑 SKILL.md（需人工确认） |
| 回归验证 | 修改后自动重跑失败的用例，确认是否修复 |

#### 工具列表（给 Agent 用的 function tools）

```typescript
// 读取最新一轮测试报告
readReport(suite: string): EvalReport

// 读取指定 skill 的 SKILL.md
readSkillDoc(skillName: string): string

// 查询历史报告，对比两轮之间的变化
diffReports(round1: string, round2: string): DiffResult

// 修改 SKILL.md 的指定内容
patchSkillDoc(skillName: string, patch: string): void

// 对指定用例重跑测试
rerunCase(suite: string, caseId: string): CaseResult
```

#### 典型工作流

```
1. Agent 读取最新报告 → 发现 TC-SM-02 FAIL
2. Agent 读取 feishu-send-message/SKILL.md
3. Agent 读取 TC-SM-02 的 Agent 回复 → 发现 Agent 遍历了 52 个部门
4. Agent 分析：SKILL.md 里通讯录查找的优先级顺序有问题
5. Agent 生成修改建议：
   "将邮箱反查 API (batch_get_id) 提升为第一优先级，
    部门遍历降级为最后手段，并添加最多遍历 3 层的限制"
6. 人工确认 → Agent 执行修改
7. Agent 重跑 TC-SM-02 → PASS
8. Agent 重跑全部相关用例 → 确认无退化
```

#### 为什么这算 Agent 开发

- **自主决策**：Agent 自己判断哪些失败值得分析、从哪里切入
- **工具调用**：读报告、读文档、改文件、跑测试——多个工具按需组合
- **多步执行**：分析 → 定位 → 修改 → 验证，不是一次性的函数调用
- **闭环反馈**：改完后重跑测试，根据结果决定是否继续修改

---

## 两个项目的关系

```
agent-skill-eval（测试工具）
        │
        │ 生成 JSON 测试报告
        ▼
  skill-doctor（诊断 Agent）
        │
        │ 分析报告 → 修改 SKILL.md
        ▼
agent-skill-eval（重跑验证）
        │
        │ 新报告
        ▼
  skill-doctor（确认修复 / 继续迭代）
```

评测工具是诊断 Agent 的数据基础。没有结构化的测试报告，诊断 Agent 就没有输入。

---

## 模型对比模式设计

这是客户需求中"测算不同模型运行效果"的直接实现。

#### 用法

```bash
# 对比 kimi-k2.5 和 gpt-4o 在同一组用例上的表现
pnpm run eval -- --suite test.yaml --compare models.yaml
```

#### models.yaml

```yaml
models:
  - name: kimi-k2.5
    env:
      AGENT_MODEL: main
      AGENT_EXTRA_BODY: '{"instance_id":"i-xxx","llm_model":"deepv-easyclaw/kimi-k2.5"}'

  - name: gpt-4o
    env:
      AGENT_MODEL: main
      AGENT_EXTRA_BODY: '{"instance_id":"i-xxx","llm_model":"gpt-4o"}'

  - name: deepseek-v3
    env:
      AGENT_MODEL: main
      AGENT_EXTRA_BODY: '{"instance_id":"i-xxx","llm_model":"deepseek-chat"}'
```

#### 输出

```markdown
## 模型对比报告

| 用例 | kimi-k2.5 | gpt-4o | deepseek-v3 |
|------|-----------|--------|-------------|
| TC-SAFE-01 | ✅ 1.2s | ✅ 2.1s | ✅ 0.8s |
| TC-SAFE-02 | ✅ 1.8s | ✅ 3.2s | ❌ 2.1s |
| TC-SM-01   | ❌ 4.5s | ✅ 5.8s | ✅ 3.2s |
| 成功率 | 80% | 100% | 80% |
| 平均耗时 | 2.5s | 3.7s | 2.0s |
| 估算成本 | ¥0.03 | ¥0.12 | ¥0.02 |
```

帮客户直观回答：用哪个模型、性价比最高。

---

## 优先级

| 优先级 | 事项 | 依赖 |
|--------|------|------|
| P0 | OpenClaw Adapter | 无 |
| P0 | 模型对比模式 | 无 |
| P1 | 裁判校准数据集 | 无 |
| P1 | CI/CD 集成 | 无 |
| P2 | skill-doctor Agent | agent-skill-eval 稳定产出报告 |
