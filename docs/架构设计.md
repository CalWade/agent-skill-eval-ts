# 架构与功能设计全介绍

## 1. 整体架构

项目是一个**全栈单体应用**，后端与前端通过 HTTP 通信，Vite 代理解决跨域问题。

```
┌──────────────────────────────────────────────────────────────┐
│                         Browser                              │
│  Vue 3 (Vite + Element Plus + ECharts)   port :5173         │
│  ConfigPanel │ SuitePanel │ LiveLog │ ResultDashboard        │
└────────────────────────┬─────────────────────────────────────┘
                         │ HTTP / SSE
                         │ (Vite dev proxy → :3001)
┌────────────────────────▼─────────────────────────────────────┐
│                  Express API Server  port :3001              │
│  /api/config  /api/models  /api/suites  /api/suite          │
│  /api/run (SSE)  /api/reports  /api/report                  │
│                         │                                    │
│  ┌──────────────────────▼──────────────────────────────────┐ │
│  │             services/runService.ts                      │ │
│  │         agent.ts  →  judge.ts                          │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────┬────────────────────────┬──────────────────┘
                   │ Cloud 模式             │ Local 模式
                   ▼                        ▼
         Agent API（OpenAI 兼容）   OpenClaw Gateway
         切换模型：发 switchCmd     切换模型：model 字段
         上下文：平台管理           上下文：sessionKey 隔离
```

### 技术选型

| 层级 | 选型 | 说明 |
|------|------|------|
| 后端运行时 | Node.js + tsx | 直接运行 TypeScript，无需编译步骤 |
| 后端框架 | Express | 轻量，SSE 实现简单直接 |
| 前端框架 | Vue 3 + Vite | Composition API + setup script |
| 前端 UI | Element Plus | 表单/对话框/表格组件齐全 |
| 前端图表 | ECharts | 通过率柱状图 + 耗时柱状图 |
| 类型共享 | 手动镜像 | `server/types.ts` ↔ `web/src/types/index.ts` |
| 样式 | CSS scoped + 全局覆盖 | 深色主题，#0f1117 背景 |
| 持久化 | 文件系统 | `.env` / `models.json` / YAML / JSON 报告 |
| 实时推流 | SSE（Server-Sent Events）| 单向推送，比 WebSocket 实现简单 |

---

## 2. 后端模块详解

### 2.1 `server/api.ts` — Express 服务器

路由注册与请求/响应处理，业务逻辑委托给 `services/` 层。

**POST /api/run（SSE 测试执行）**：

设置 `text/event-stream` 头后立即 `flushHeaders()`，通过 `send(event, data)` 写入事件流：

```
对每个模型：
  1. emit model_start
  2. Cloud：发 switchCmd → callAgent() → emit switch_ok / switch_fail
     Local：跳过切换步骤，emit switch_ok（durationMs=0）
  3. 切换失败 → 该模型所有用例标记 FAIL，continue
  4. 对每个用例：
       Cloud：callAgent(instruction, cloudConfig)
       Local：callAgent(instruction, { ...localConfig, localModelId, sessionKey })
       → judge() → emit case_result
  5. emit model_done
完成：构建 EvalReport → 写 JSON 文件 → emit done
```

**配置读写**（`POST /api/config`）：

采用**合并写入**策略：前端只发送当前模式可见的字段，服务端读取现有 `.env` 后与新字段合并，再整体写回，避免切换模式时另一模式的配置被清空。

### 2.2 `server/services/configService.ts` — 配置服务

```typescript
readEnvFile()   // 读 .env → Record<string, string>
writeEnvFile()  // 合并写入 .env + 同步 process.env
loadConfig()    // 从 process.env 构建 AppConfig，按 mode 分支校验
```

`loadConfig()` 按模式分支：

- **cloud 模式**：校验 `AGENT_API_URL` 和 `AGENT_API_KEY` 必填
- **local 模式**：校验 `LOCAL_TOKEN` 必填，`LOCAL_BASE_URL` 默认 `http://127.0.0.1:18789`

`AppConfig` 结构：

```typescript
interface AppConfig {
  mode: 'cloud' | 'local';
  // cloud
  agentApiUrl: string;
  agentApiKey: string;
  agentExtraBody: Record<string, unknown>;
  switchWaitMs: number;
  // local
  localBaseUrl: string;
  localToken: string;
  // 通用
  intervalMs: number;
  timeoutMs: number;
  resultsDir: string;
}
```

### 2.3 `server/services/modelService.ts` — 模型服务

两层 fallback：`models.json`（用户自定义）→ `server/models.ts`（硬编码）。

`models.json` 格式：

```json
{
  "prefix": "/model",
  "ids": ["kimi-k2.5", "claude-haiku-4-5"],
  "localModels": {
    "kimi-k2.5": "qingshu/gpt-5.3",
    "claude-haiku-4-5": "custom-dashscope/qwen3.5-plus"
  }
}
```

- `prefix` + `id` 动态拼接 `switchCmd`（cloud 模式用）
- `localModels[id]` 提供 `localModelId`（local 模式用）
- 保存时过滤掉 `localModels` 中不再存在的 id

### 2.4 `server/services/runService.ts` — 执行引擎

SSE 路由和 CLI 共用的核心逻辑，通过回调接口解耦输出方式：

```typescript
interface RunCallbacks {
  onModelStart?, onSwitch?, onSwitchOk?, onSwitchFail?,
  onCaseStart?, onCaseResult?, onModelDone?
}
```

**Local 模式执行逻辑**：

```typescript
const isLocal = config.agent.mode === 'local';
const runId = config.runId ?? Date.now().toString(36);

// 每个用例注入独立 sessionKey
const agentConfig = isLocal ? {
  ...config.agent,
  localModelId: model.localModelId,
  sessionKey: `eval:${runId}:${model.id}:${c.id}`,
} : config.agent;
```

`runId` 由调用方（`api.ts` / `cli.ts`）在启动时生成，同一批次所有用例共享，跨批次不同，保证不同测试轮次完全隔离。

### 2.5 `server/agent.ts` — Agent 调用

支持 cloud / local 双模式，统一入口 `callAgent()` 内部自动分派：

**Cloud 模式（`callAgentCloud`）**：

- 标准 OpenAI 格式：`{ messages, stream: false, ...extraBody }`
- 每次只发当前 user 消息（无状态单轮）
- `AbortController` 控制超时

**Local 模式（`callAgentLocal`）**：

- 端点：`{localBaseUrl}/v1/chat/completions`
- 请求体：`{ model: localModelId, messages, stream: false }`
- 额外 header：
  - `Authorization: Bearer {localToken}`
  - `x-openclaw-agent-id: main`
  - `x-openclaw-session-key: {sessionKey}`（上下文隔离关键）

**公共解析逻辑（`parseOpenAIResponse`）**：

两种模式响应体均为标准 OpenAI 格式，共用同一解析函数：
- 提取 `choices[0].message.content`
- Token 统计：OpenClaw 返回全为 0，统一置 `null` 避免误导报告

所有分支都返回确定性 `CallResult`，不 throw。

### 2.6 `server/judge.ts` — 判定引擎

三种文本匹配，无 LLM-as-Judge：

| `type` | 逻辑 | 判定影响 |
|--------|------|---------|
| `output_contains` | `output.includes(text)` | 不包含 → failReasons |
| `output_not_contains` | `!output.includes(text)` | 包含 → failReasons |
| `output_contains_any` | `texts.some(t => output.includes(t))` | 全不含 → failReasons |

`pass_criteria` 为空 → `DISPLAY`；`failReasons` 为空 → `PASS`；否则 `FAIL`。

### 2.7 `server/runner.ts` + `server/report.ts` — CLI 适配

`runner.ts`：调用 `runService.executeRun()`，通过回调将进度输出到 stdout。

`report.ts`：控制台格式化输出（`printModelHeader` / `printCaseResult` 等）+ 生成 Markdown 报告文件。

---

## 3. 前端模块详解

### 3.1 整体布局

```
┌─ header（顶栏）─────────────────────────────────────────┐
│  [AGENT SKILL EVAL]  │  STATUS  │  时钟  │  HISTORY 按钮 │
├─ zone-a（300px）──┬─ zone-b（flex:1.4）┬─ zone-c（flex:1）┤
│  ConfigPanel      │  CasePanel        │  LiveLog         │
│  SuitePanel       │                   │  ProgressBar     │
│  [START TEST]     │                   │                  │
└───────────────────┴───────────────────┴──────────────────┘
```

History 通过右侧抽屉（`el-drawer`）展示。报告大屏通过路由 `/report` 独立页面展示。

### 3.2 `ConfigPanel.vue`

模式切换 radio group（Cloud / Local），根据 `agentMode` 条件渲染不同字段：

- Cloud：API URL、API Key、Instance ID、切换等待
- Local：Gateway URL、Gateway Token

**保存时始终发送完整表单**（包含两种模式的字段），后端合并写入，避免切换模式后另一模式配置丢失。

### 3.3 `SuitePanel.vue`

模型编辑对话框新增 Local 模型 ID 列（双列布局）：

```
模型名         │  Local: providerId/modelId
────────────── │ ────────────────────────────
kimi-k2.5      │  qingshu/gpt-5.3
claude-haiku   │  custom-dashscope/qwen3.5-plus
```

保存时调用 `saveModels(prefix, ids, localModels)` 同时更新三个字段。

### 3.4 `web/src/api/index.ts`

```typescript
getModels()  // 返回 { models, prefix, ids, localModels }
saveModels(prefix, ids, localModels?)  // 第三个参数可选
getConfig()  // 返回含 agentMode / localBaseUrl / localToken 的完整配置
saveConfig() // 发送完整配置对象
```

### 3.5 `web/src/types/index.ts`

新增类型：

```typescript
type AgentMode = 'cloud' | 'local'

interface ModelConfig {
  id: string
  switchCmd: string
  localModelId?: string   // local 模式专用
}
```

---

## 4. 数据流

### 4.1 启动测试完整流程

```
用户点击「开始测试」
  → api.startRun() → POST /api/run { modelIds, suite }
  → Express：
      ① loadConfig() 确定 mode
      ② getModels() 获取含 localModelId 的完整列表
      ③ runService.executeRun()：
           Cloud：switchCmd → callAgent(cloud) → judge
           Local：skip switch → callAgent(local, sessionKey) → judge
           每条用例 → SSE case_result
      ④ buildReport() → 写 JSON → SSE done
  → 前端：SSE 事件 → addLog() + ResultDashboard
```

### 4.2 配置保存流程

```
用户修改配置 → 点保存
  → POST /api/config（完整表单，含两种模式字段）
  → writeEnvFile()：readEnvFile() merge 新数据 → 写 .env + 更新 process.env
  → 下次 loadConfig() 立即生效，无需重启
```

### 4.3 持久化文件一览

| 文件 | 格式 | 写入时机 | 读取时机 |
|------|------|---------|---------|
| `.env` | KEY=VALUE | 保存 API 配置时（合并写入） | 每次 POST /api/run 前 |
| `models.json` | JSON `{ prefix, ids, localModels? }` | 保存模型列表时 | GET /api/models、POST /api/run |
| `examples/**/*.yaml` | YAML | 保存测试套件时 | 选择套件时、启动测试时 |
| `results/*.json` | JSON（EvalReport）| 每次测试完成后 | 历史报告页面点击时 |

---

## 5. 核心类型

```typescript
type AgentMode = 'cloud' | 'local'

interface ModelConfig {
  id: string;
  switchCmd: string;       // cloud 模式切换指令
  localModelId?: string;   // local 模式 OpenClaw model 字段值
}

type Verdict = 'PASS' | 'FAIL' | 'DISPLAY'

interface CallResult {
  success: boolean;
  output: string;
  durationMs: number;
  promptTokens: number | null;    // OpenClaw local 模式始终为 null
  completionTokens: number | null;
  totalTokens: number | null;
  finishReason: string | null;
  error: string | null;
}

interface CaseModelResult {
  caseId: string; caseTitle: string; modelId: string;
  call: CallResult;
  verdict: Verdict;
  failReasons: string[];
}

interface EvalReport {
  skill: string; description: string; timestamp: string;
  modelIds: string[];
  cases: Array<{ id; title; instruction; sideEffect }>;
  results: CaseModelResult[];
}
```

---

## 6. 设计决策

**Local 模式为何用 sessionKey 而非新建 agent？**

OpenClaw 的 session 机制天然支持按 key 隔离上下文，无需为每个用例创建独立 agent 实例。格式 `eval:{runId}:{modelId}:{caseId}` 三层隔离：批次间（runId）、模型间（modelId）、用例间（caseId）。

**Local 模式为何不需要 switchCmd？**

OpenClaw 通过请求体 `model` 字段直接指定模型，不需要发消息触发切换，因此 local 模式跳过整个 switch 步骤，`switchWaitMs` 也无意义。

**writeEnvFile 为何改为合并写入？**

前端 ConfigPanel 根据当前模式只渲染/发送对应字段，若覆盖写入会清空另一模式的配置。合并写入保留历史配置，两种模式可以自由切换而不丢失各自的字段。

**为何用 SSE 而非 WebSocket？**

测试执行是单向推送（服务器 → 浏览器），SSE 是 HTTP 原生支持的单向流，实现更简单，不需要维护双向连接状态。

**为何不用全局状态管理（Pinia）？**

数据流清晰：`MainPage.vue` 是唯一状态协调点，子组件通过 props/emit 通信，不需要全局 store。
